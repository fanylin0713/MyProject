import { handleOpen, handleClose, setTitle } from './actions';

export const HANDLE_OPEN = handleOpen().type;
export const HANDLE_CLOSE = handleClose().type;
export const SET_TITLE = setTitle().type;
